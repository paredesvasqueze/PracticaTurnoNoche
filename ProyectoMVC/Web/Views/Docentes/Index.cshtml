@model IEnumerable<Models.Docentes>

<div class="container mt-4">
    <h2 class="text-center mb-3">Gestión de Docentes</h2>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    <div class="text-end mb-3">
        <a asp-action="Create" class="btn btn-primary">➕ Nuevo Docente</a>
    </div>

    <table class="table table-bordered table-striped">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Nombres</th>
                <th>Apellidos</th>
                <th>DNI</th>
                <th>Teléfono</th>
                <th>Especialidad</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Any())
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.IdDocente</td>
                        <td>@item.Nombres</td>
                        <td>@item.Apellidos</td>
                        <td>@item.DNI</td>
                        <td>@item.Telefono</td>
                        <td>@item.Especialidad</td>
                        <td>@(item.Estado ? "Activo" : "Inactivo")</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.IdDocente" class="btn btn-sm btn-warning">Editar</a>
                            <a asp-action="Delete" asp-route-id="@item.IdDocente" class="btn btn-sm btn-danger">Eliminar</a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr><td colspan="8" class="text-center text-muted">No hay docentes registrados.</td></tr>
            }
        </tbody>
    </table>
</div>
